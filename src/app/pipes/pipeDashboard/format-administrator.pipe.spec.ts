import { FormatAdministratorPipe } from './format-administrator.pipe';

describe('FormatAdministratorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatAdministratorPipe();
    expect(pipe).toBeTruthy();
  });
});
